import 'package:flutter/material.dart';
import 'package:pomotimer/pages/main_page.dart';

void main() => runApp(new Blob());

class Blob extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new MaterialApp(
      home: new HomePage(),
    );
  }
}
